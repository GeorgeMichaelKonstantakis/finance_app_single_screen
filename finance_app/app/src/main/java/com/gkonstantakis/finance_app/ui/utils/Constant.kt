package com.gkonstantakis.finance_app.ui.utils

class Constant {
    companion object {
        // These values would probably come from an API call, which we would then save to SQLite, for now we set them as constants
        val totalAvailableAmount = 25378
        val basketStepValue = 2917
    }
}